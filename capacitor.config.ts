import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reactexample.app',
  appName: 'react-example',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
