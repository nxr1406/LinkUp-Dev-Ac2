name: lan_ip_camera
description: LAN IP Camera System - Stream live video over local WiFi/Hotspot
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  network_info_plus: ^6.0.0
  permission_handler: ^11.3.1
  wakelock_plus: ^1.2.8
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
